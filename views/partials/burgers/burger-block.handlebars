<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-devour="{{devour}}">
		{{#if devour}}  BURGER TIME!{{else}}NOT HUNGRY!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
